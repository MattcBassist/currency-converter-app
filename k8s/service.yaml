apiVersion: v1
kind: Service
metadata:
  name: currency-converter-service
spec:
  type: NodePort
  selector:
    app: currency-converter
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30080
